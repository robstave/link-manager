services:
  db:
    image: pgvector/pgvector:pg17-bookworm
    environment:
      POSTGRES_DB: linkmgr
      POSTGRES_USER: linkmgr
      POSTGRES_PASSWORD: linkmgr
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U linkmgr -d linkmgr" ]
      interval: 5s
      timeout: 3s
      retries: 20

  migrate:
    build:
      context: .
      dockerfile: Dockerfile.migrate
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://linkmgr:linkmgr@db:5432/linkmgr?sslmode=disable
    volumes:
      - ./migrations:/migrations:ro
    command: [ "-dir=/migrations", "postgres", "postgres://linkmgr:linkmgr@db:5432/linkmgr?sslmode=disable", "up" ]
    restart: "no"

  api:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      migrate:
        condition: service_completed_successfully
    environment:
      DATABASE_URL: postgres://linkmgr:linkmgr@db:5432/linkmgr?sslmode=disable
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin}
      PORT: 8080
    ports:
      - "8080:8080"

  web-react:
    image: node:20-alpine
    depends_on:
      api:
        condition: service_started
    working_dir: /app
    volumes:
      - ./web-react:/app
      - /app/node_modules
    environment:
      VITE_API_PROXY_TARGET: http://api:8080
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5177"
    ports:
      - "5177:5177"

  web-legacy:
    image: nginx:alpine
    profiles: ["legacy-ui"]
    depends_on:
      api:
        condition: service_started
    volumes:
      - ./web:/usr/share/nginx/html:ro
      - ./web/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "5178:80"

volumes:
  pgdata:
